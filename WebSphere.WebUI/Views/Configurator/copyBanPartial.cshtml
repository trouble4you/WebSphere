@model bool
<div id="addControllerNodeModal" class="modal fade" tabindex="-1" style="top:100px;">
    <div class=" modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button class="close" type="button" data-dismiss="modal">x</button>
                <h4 class="modal-title">Предупреждение</h4>
            </div>
            <div class="modal-body">
                <div class="tab-content">
                    @if(Model==true)
                    {
                         <p>Происходит копирование. Вставка и копирование временно не доступны.</p>  
                    }
                    else
                    {
                         <p>Копирование невозможно. Обновите конфигурацию.</p>  
                    }

                </div>
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">

    $(document).ready(function () {
        checkCreateNode = 0;
        checkCopyNode = 0;
        $('#addControllerNodeModal').modal();
        function func() {
            $('#addControllerNodeModal').modal("hide");
            $('#dialogToAddModule').empty;
        }
        setTimeout(func, 3000);
    });




</script>
